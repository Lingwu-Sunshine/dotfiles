#+title: 存放i3的壁纸
#+startup: latexpreview
#+author: 灵灵

1. 在 ~i3~ 此文件夹的位置( =~/.config/wallpaper= )配置信息为:
   #+BEGIN_SRC yaml
     exec --no-startup-id feh --randomize --bg-fill ~/.config/wallpaper
   #+END_SRC
2. 将字体文件 ~fonts~ 移至到 ~$HOME/.local/~ 目录下，然后刷新字体库，命令为 ~fc-cache~ 或者 ~fc-cache -vf~
